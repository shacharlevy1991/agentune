name: Agentune-analyze-main

on:
  pull_request:
    branches:
      - main
    paths:
      - 'agentune_analyze/**'
  push:
    branches:
      - main
    paths:
      - 'agentune_analyze/**'

permissions:
  contents: write
  pull-requests: write
  id-token: write
  packages: write

jobs:
  python_tests:
    uses: ./.github/workflows/python_style.yml
    with:
      src_path: "agentune_analyze"


